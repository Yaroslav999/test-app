%h1 Users

%h2 Local Users
%ul
  - @users.each do |user|
    %li= "#{user.full_name} - #{user.email}"

%h2 External Users
%ul
  - @external_users.each do |user|
    %li= "#{user[:name]} - #{user[:email]}"
